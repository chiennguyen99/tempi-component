'use strict';

Object.defineProperty(exports, '__esModule', { value: true });

var React = require('react');
var mapping = require('../mapping.js');

const useIcon = (name, isVisible) => {
  const [error, setError] = React.useState(null);
  const [currIcon, setCurrIcon] = React.useState(null);
  React.useEffect(() => {
    const fetchImage = async () => {
      if (!isVisible || !name)
        return;
      try {
        const resIcon = await mapping.getIconUrl(name);
        if (!resIcon)
          throw new Error("icon notFound");
        if (resIcon !== currIcon)
          setCurrIcon(resIcon);
      } catch (err) {
        setError(err);
      }
    };
    fetchImage();
  }, [name, isVisible]);
  return {
    error,
    icon: currIcon
  };
};

exports.useIcon = useIcon;
