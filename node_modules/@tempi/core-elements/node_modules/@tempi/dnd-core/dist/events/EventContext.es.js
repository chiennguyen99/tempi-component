import { createContext, useContext } from 'react';

const EventHandlerContext = createContext(null);
const useEventHandler = () => useContext(EventHandlerContext);

export { EventHandlerContext, useEventHandler };
