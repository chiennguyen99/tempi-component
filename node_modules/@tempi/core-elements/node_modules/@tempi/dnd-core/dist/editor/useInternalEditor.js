'use strict';

Object.defineProperty(exports, '__esModule', { value: true });

var dndUtils = require('@tempi/dnd-utils');
var React = require('react');
var invariant = require('tiny-invariant');
var EditorContext = require('./EditorContext.js');
var EventContext = require('../events/EventContext.js');

function _interopDefaultLegacy (e) { return e && typeof e === 'object' && 'default' in e ? e : { 'default': e }; }

var invariant__default = /*#__PURE__*/_interopDefaultLegacy(invariant);

var __defProp = Object.defineProperty;
var __defProps = Object.defineProperties;
var __getOwnPropDescs = Object.getOwnPropertyDescriptors;
var __getOwnPropSymbols = Object.getOwnPropertySymbols;
var __hasOwnProp = Object.prototype.hasOwnProperty;
var __propIsEnum = Object.prototype.propertyIsEnumerable;
var __defNormalProp = (obj, key, value) => key in obj ? __defProp(obj, key, { enumerable: true, configurable: true, writable: true, value }) : obj[key] = value;
var __spreadValues = (a, b) => {
  for (var prop in b || (b = {}))
    if (__hasOwnProp.call(b, prop))
      __defNormalProp(a, prop, b[prop]);
  if (__getOwnPropSymbols)
    for (var prop of __getOwnPropSymbols(b)) {
      if (__propIsEnum.call(b, prop))
        __defNormalProp(a, prop, b[prop]);
    }
  return a;
};
var __spreadProps = (a, b) => __defProps(a, __getOwnPropDescs(b));
function useInternalEditor(collector) {
  const handler = EventContext.useEventHandler();
  const store = React.useContext(EditorContext.EditorContext);
  invariant__default["default"](store, dndUtils.ERROR_USE_EDITOR_OUTSIDE_OF_EDITOR_CONTEXT);
  const collected = dndUtils.useCollector(store, collector);
  const connectorsUsage = React.useMemo(
    () => handler && handler.createConnectorsUsage(),
    [handler]
  );
  React.useEffect(() => {
    connectorsUsage.register();
    return () => {
      connectorsUsage.cleanup();
    };
  }, [connectorsUsage]);
  const connectors = React.useMemo(
    () => connectorsUsage && dndUtils.wrapConnectorHooks(connectorsUsage.connectors),
    [connectorsUsage]
  );
  return __spreadProps(__spreadValues({}, collected), {
    connectors,
    inContext: !!store,
    store
  });
}

exports.useInternalEditor = useInternalEditor;
