import { jsx } from 'react/jsx-runtime';
import { UnitEnum } from '@tempi/core-renderer';
import { Select } from '../SelectConfig/SelectConfig.es.js';

const UnitConfig = ({
  propKey,
  options = [],
  defaultValue = UnitEnum.pixel,
  onChange
}) => {
  return /* @__PURE__ */ jsx(Select, {
    style: { width: 60 },
    propKey,
    defaultValue,
    options: options.map((unit) => ({
      value: unit,
      label: unit
    })),
    onChange
  });
};

export { UnitConfig };
