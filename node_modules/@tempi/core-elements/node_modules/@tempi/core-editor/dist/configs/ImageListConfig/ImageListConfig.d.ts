import { Image as ImageInterface } from '@tempi/core-renderer';
import { ModalConfigProps } from '../../components';
import { SettingConfigProps } from '../../types';
import { Action } from './SortableImage';
export type ImageListProps<T = {}> = SettingConfigProps & ModalConfigProps & {
    imageHeight?: number;
    actions?: Action[];
    defaultNewImage?: T;
};
export declare const ImageListConfig: <T extends ImageInterface>({ propKey, separateDevice, visible, setVisible, imageHeight, actions, defaultNewImage, }: ImageListProps<T>) => JSX.Element;
//# sourceMappingURL=ImageListConfig.d.ts.map