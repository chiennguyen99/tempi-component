import { jsx } from 'react/jsx-runtime';
import { Settings16Regular } from '@fluentui/react-icons';
import { useNode } from '@tempi/dnd-core';
import { QuickSettingButton } from '../../components/QuickSettingButton/QuickSettingButton.es.js';
import { useBuilder } from '../../contexts/BuilderContext.es.js';
import { useEditor } from '../../hooks/useEditor.es.js';

const OpenSettingDialog = ({ children }) => {
  const { setShowSettingDialog } = useBuilder();
  const { actions } = useEditor();
  const { id } = useNode();
  return /* @__PURE__ */ jsx(QuickSettingButton, {
    onClick: () => {
      actions.selectNode(id);
      setShowSettingDialog(true);
    },
    children: !!children ? children : /* @__PURE__ */ jsx(Settings16Regular, {})
  });
};

export { OpenSettingDialog };
