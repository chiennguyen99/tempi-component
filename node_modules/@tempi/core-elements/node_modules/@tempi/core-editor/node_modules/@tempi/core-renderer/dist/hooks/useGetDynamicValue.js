'use strict';

Object.defineProperty(exports, '__esModule', { value: true });

var get = require('lodash.get');
var PageDataContext = require('../contexts/PageDataContext.js');

function _interopDefaultLegacy (e) { return e && typeof e === 'object' && 'default' in e ? e : { 'default': e }; }

var get__default = /*#__PURE__*/_interopDefaultLegacy(get);

const useGetDynamicValue = () => {
  const { getValueByPath } = PageDataContext.usePageData();
  const getValue = (valueConfig, extraDataSrc) => {
    const { valueType, contextPath, staticValue } = valueConfig || {};
    if (valueType === "STATIC" && staticValue)
      return staticValue;
    if (valueType === "CONTEXT_PATH" && contextPath) {
      return get__default["default"](extraDataSrc, contextPath) || getValueByPath(contextPath);
    }
  };
  return { getValue };
};

exports.useGetDynamicValue = useGetDynamicValue;
