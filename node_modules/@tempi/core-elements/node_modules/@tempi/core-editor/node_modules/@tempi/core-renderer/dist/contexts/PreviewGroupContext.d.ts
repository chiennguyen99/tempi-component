import React from 'react';
import { Image } from '../types';
export interface IPreviewGroupState {
    images?: Image[];
    currentIndex?: number;
    setCurrentIndex?: React.Dispatch<React.SetStateAction<number>>;
}
export type PreviewGroupProviderProps = {
    children: React.ReactNode;
    images?: Image[];
};
export declare const PreviewGroupProvider: ({ children, images, }: PreviewGroupProviderProps) => JSX.Element;
export declare const usePreviewGroup: () => IPreviewGroupState;
//# sourceMappingURL=PreviewGroupContext.d.ts.map