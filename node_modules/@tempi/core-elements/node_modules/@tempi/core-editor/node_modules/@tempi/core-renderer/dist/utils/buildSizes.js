'use strict';

Object.defineProperty(exports, '__esModule', { value: true });

var isAbsoluteWidth = require('./isAbsoluteWidth.js');

const buildSizes = (w) => {
  if (isAbsoluteWidth.isAbsoluteWidth(w))
    return `${w.value}${w.unit}`;
  return `${w.value}vw`;
};

exports.buildSizes = buildSizes;
