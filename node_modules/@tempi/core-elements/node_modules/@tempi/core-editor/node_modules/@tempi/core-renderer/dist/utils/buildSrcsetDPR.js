'use strict';

Object.defineProperty(exports, '__esModule', { value: true });

var image = require('./image.js');
var root = require('../constants/root.js');

const buildSrcsetDPR = (imageUrl, absolutedWidth) => {
  if (!imageUrl)
    return "";
  if (!imageUrl.startsWith("https://lh3.googleusercontent.com/"))
    return imageUrl;
  return Array.from({ length: root.MAX_DPR }, (_, i) => i + 1).map(
    (dpr) => `${image.constructGoogleImageParams(imageUrl, {
      width: absolutedWidth * dpr,
      format: "webp"
    })} ${dpr}x`
  ).join(", ");
};

exports.buildSrcsetDPR = buildSrcsetDPR;
