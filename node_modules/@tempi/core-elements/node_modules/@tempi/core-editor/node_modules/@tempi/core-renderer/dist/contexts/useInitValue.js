'use strict';

Object.defineProperty(exports, '__esModule', { value: true });

var React = require('react');
var PageDataContext = require('./PageDataContext.js');

const useInitValue = ({
  settingValue
}) => {
  const { storeData, getValueByPath } = PageDataContext.usePageData();
  const initValue = React.useMemo(() => {
    const { valueType, contextPath, staticValue } = settingValue || {};
    let value;
    if (valueType === "STATIC") {
      value = staticValue;
    }
    if (valueType === "CONTEXT_PATH") {
      value = getValueByPath(contextPath);
    }
    return value;
  }, [settingValue, storeData == null ? void 0 : storeData.components]);
  return {
    initValue
  };
};

exports.useInitValue = useInitValue;
