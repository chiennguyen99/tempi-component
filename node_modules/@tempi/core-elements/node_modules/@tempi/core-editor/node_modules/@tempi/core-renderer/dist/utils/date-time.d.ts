import { TimeDelta } from '../types';
export declare const parseRuleRepeat: (field: string, ruleRepeat: string) => string;
export declare const validateIsVisibleComponent: ({ startTime, endTime, ruleRepeat, date, duration, }: {
    startTime?: string;
    endTime?: string;
    ruleRepeat?: string;
    date?: string;
    duration?: number[];
}) => boolean;
export declare const calculateTimeDelta: (startTime: Date, endTime: Date) => TimeDelta;
//# sourceMappingURL=date-time.d.ts.map