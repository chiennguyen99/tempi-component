'use strict';

Object.defineProperty(exports, '__esModule', { value: true });

var hexRgb = require('./core/parse-css-color/utils/hex-rgb.js');

function getRgbaFromHex(hex, alpha = 1) {
  var _a;
  const rgb = hexRgb.hex2Rgb(hex);
  if (!rgb) {
    return null;
  }
  return `rgba(${rgb.red},${rgb.green},${rgb.blue},${(_a = rgb == null ? void 0 : rgb.alpha) != null ? _a : alpha})`;
}

exports.getRgbaFromHex = getRgbaFromHex;
