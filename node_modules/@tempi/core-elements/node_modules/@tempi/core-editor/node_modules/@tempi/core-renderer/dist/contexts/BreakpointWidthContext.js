'use strict';

Object.defineProperty(exports, '__esModule', { value: true });

var jsxRuntime = require('react/jsx-runtime');
var React = require('react');
var isAbsoluteWidth = require('../utils/isAbsoluteWidth.js');

const BreakpointWidthContext = React.createContext({
  sWidth: {
    value: 100,
    unit: "%"
  },
  mWidth: {
    value: 100,
    unit: "%"
  }
});
const getAggregateWidth = (currWidth, aggWidth) => {
  if (isAbsoluteWidth.isAbsoluteWidth(currWidth))
    return currWidth;
  return {
    value: aggWidth.value * currWidth.value / 100,
    unit: isAbsoluteWidth.isAbsoluteWidth(aggWidth) ? "px" : "%"
  };
};
const BreakpointWidthProvider = ({ children, sWidth, mWidth }) => {
  const { sWidth: aggregateSWidth, mWidth: aggregateMWidth } = useBreakpointWidth();
  return /* @__PURE__ */ jsxRuntime.jsx(BreakpointWidthContext.Provider, {
    value: {
      sWidth: getAggregateWidth(sWidth, aggregateSWidth),
      mWidth: getAggregateWidth(mWidth, aggregateMWidth)
    },
    children
  });
};
const useBreakpointWidth = () => {
  return React.useContext(BreakpointWidthContext);
};

exports.BreakpointWidthProvider = BreakpointWidthProvider;
exports.useBreakpointWidth = useBreakpointWidth;
