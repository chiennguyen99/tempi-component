import { jsx } from 'react/jsx-runtime';
import styled from '@emotion/styled';
import { Tabs } from 'antd';

const QuickSettingTabWrapper = ({
  children,
  centered = true
}) => {
  return /* @__PURE__ */ jsx(TabWrapper, {
    centered,
    children
  });
};
const TabWrapper = styled(Tabs)`
  overflow: unset;
`;

export { QuickSettingTabWrapper };
