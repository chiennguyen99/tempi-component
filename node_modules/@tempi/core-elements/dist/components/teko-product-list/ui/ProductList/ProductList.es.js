import { jsx } from 'react/jsx-runtime';
import React__default from 'react';
import styled from '@emotion/styled';
import { getStyle } from '@tempi/core-renderer';
import { useProductList, ProductListProvider } from '../../context.es.js';
import { LazyComponent } from '../../../../core/LazyComponent/LazyComponent.es.js';

var __defProp = Object.defineProperty;
var __getOwnPropSymbols = Object.getOwnPropertySymbols;
var __hasOwnProp = Object.prototype.hasOwnProperty;
var __propIsEnum = Object.prototype.propertyIsEnumerable;
var __defNormalProp = (obj, key, value) => key in obj ? __defProp(obj, key, { enumerable: true, configurable: true, writable: true, value }) : obj[key] = value;
var __spreadValues = (a, b) => {
  for (var prop in b || (b = {}))
    if (__hasOwnProp.call(b, prop))
      __defNormalProp(a, prop, b[prop]);
  if (__getOwnPropSymbols)
    for (var prop of __getOwnPropSymbols(b)) {
      if (__propIsEnum.call(b, prop))
        __defNormalProp(a, prop, b[prop]);
    }
  return a;
};
var __objRest = (source, exclude) => {
  var target = {};
  for (var prop in source)
    if (__hasOwnProp.call(source, prop) && exclude.indexOf(prop) < 0)
      target[prop] = source[prop];
  if (source != null && __getOwnPropSymbols)
    for (var prop of __getOwnPropSymbols(source)) {
      if (exclude.indexOf(prop) < 0 && __propIsEnum.call(source, prop))
        target[prop] = source[prop];
    }
  return target;
};
const ProductListInner = React__default.forwardRef(
  (props, ref) => {
    const _a = props, { isEditorMode = false } = _a, rest = __objRest(_a, ["isEditorMode"]);
    const { showBlock } = useProductList();
    if (!isEditorMode && !showBlock)
      return null;
    return /* @__PURE__ */ jsx(StyledProductList, __spreadValues({
      ref
    }, rest));
  }
);
const ProductList = React__default.forwardRef(
  (props, ref) => {
    const WrapLazy = props.isEditorMode ? React__default.Fragment : LazyComponent;
    return /* @__PURE__ */ jsx(WrapLazy, {
      children: /* @__PURE__ */ jsx(ProductListProvider, {
        children: /* @__PURE__ */ jsx(ProductListInner, __spreadValues({
          ref
        }, props))
      })
    });
  }
);
const StyledProductList = styled.div(
  ({ commonStyle }) => `
  ${getStyle(commonStyle)}
`
);
ProductList.displayName = "TekoProductListV1";

export { ProductList };
