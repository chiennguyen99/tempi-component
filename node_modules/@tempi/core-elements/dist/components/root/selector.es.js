import { withSelector } from '@tempi/core-editor';
import { Setting } from './setting.es.js';
import { DEFAULT_ROOT } from './constants.es.js';
import { Root } from './ui.es.js';

const RootEditor = withSelector(
  Root,
  {
    displayName: "Root",
    tag: Root.displayName.toLowerCase(),
    customAttributes: Setting,
    props: DEFAULT_ROOT,
    rules: {
      canDrag: () => false
    }
  },
  {
    style: {
      paddingBottom: 400,
      overflowX: "hidden"
    }
  }
);

export { RootEditor };
