import { WidthTypeEnum } from '../../types/index.es.js';

const DEFAULT_ROOT = {
  pageWidthType: WidthTypeEnum.fixed,
  pageMaxWidth: 1232,
  commonStyle: {
    desktop: {
      backgroundSize: "cover",
      bgImagePercent: 100
    }
  }
};

export { DEFAULT_ROOT };
