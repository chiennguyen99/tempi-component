'use strict';

Object.defineProperty(exports, '__esModule', { value: true });

var coreEditor = require('@tempi/core-editor');
var setting = require('./setting.js');
var constants = require('./constants.js');
var ui = require('./ui.js');

const RootEditor = coreEditor.withSelector(
  ui.Root,
  {
    displayName: "Root",
    tag: ui.Root.displayName.toLowerCase(),
    customAttributes: setting.Setting,
    props: constants.DEFAULT_ROOT,
    rules: {
      canDrag: () => false
    }
  },
  {
    style: {
      paddingBottom: 400,
      overflowX: "hidden"
    }
  }
);

exports.RootEditor = RootEditor;
