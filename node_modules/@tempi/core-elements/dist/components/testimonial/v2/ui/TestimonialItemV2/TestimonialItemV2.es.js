import { jsx } from 'react/jsx-runtime';
import styled from '@emotion/styled';
import { getStyle } from '@tempi/core-renderer';
import React__default from 'react';

const TestimonialV2Item = React__default.forwardRef(({ children, commonStyle }, ref) => {
  return /* @__PURE__ */ jsx(StyledDiv, {
    commonStyle,
    ref,
    children
  });
});
const StyledDiv = styled.div(
  ({ commonStyle }) => `
    ${getStyle(commonStyle)}
  `
);
TestimonialV2Item.displayName = "TestimonialV2Item";

export { TestimonialV2Item };
