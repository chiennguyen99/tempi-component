import { jsx, Fragment } from 'react/jsx-runtime';
import { t } from '../../../i18n.es.js';
import { RangeSliderConfig, PROP_KEY } from '@tempi/core-editor';
import { UnitEnum } from '@tempi/core-renderer';

const SliderStyleSetting = () => {
  return /* @__PURE__ */ jsx(Fragment, {
    children: /* @__PURE__ */ jsx(RangeSliderConfig, {
      sliderProps: {
        min: 0,
        max: 100,
        defaultValue: 0,
        step: 4
      },
      formItemProps: {
        label: t("Bo g\xF3c")
      },
      propKey: PROP_KEY.joinKey(PROP_KEY.CommonStyle, PROP_KEY.BorderRadius),
      unitProps: { defaultValue: UnitEnum.percent }
    })
  });
};

export { SliderStyleSetting };
