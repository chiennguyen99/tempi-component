import { jsx } from 'react/jsx-runtime';
import { useProp } from '@tempi/core-editor';
import { SwiperSetting } from '../../../configs/Swiper/SwiperSetting.es.js';
import { PROP_KEY } from '../../../constants/propKey.es.js';

const SliderSetting = () => {
  const [allowSetupThumbnail] = useProp(PROP_KEY.AllowSetupThumbnail);
  return /* @__PURE__ */ jsx(SwiperSetting, {
    allowSetupThumbnail
  });
};

export { SliderSetting };
