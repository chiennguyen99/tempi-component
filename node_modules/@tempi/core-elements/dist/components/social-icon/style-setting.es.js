import { jsxs, Fragment, jsx } from 'react/jsx-runtime';
import { t } from '../../i18n.es.js';
import { RangeSliderConfig } from '@tempi/core-editor';
import { UnitEnum } from '@tempi/core-renderer';
import { PROP_KEY } from '../../constants/propKey.es.js';

const StyleSetting = () => {
  return /* @__PURE__ */ jsxs(Fragment, {
    children: [
      /* @__PURE__ */ jsx(RangeSliderConfig, {
        propKey: "iconStyle.width",
        formItemProps: {
          label: t("K\xEDch th\u01B0\u1EDBc khung")
        },
        sliderProps: {
          min: 10,
          max: 200
        },
        unitProps: { defaultValue: UnitEnum.pixel }
      }),
      /* @__PURE__ */ jsx(RangeSliderConfig, {
        propKey: "iconSize",
        formItemProps: {
          label: t("K\xEDch th\u01B0\u1EDBc bi\u1EC3u t\u01B0\u1EE3ng")
        },
        sliderProps: {
          min: 10,
          max: 50
        },
        unitProps: { defaultValue: UnitEnum.pixel }
      }),
      /* @__PURE__ */ jsx(RangeSliderConfig, {
        propKey: PROP_KEY.joinKey(PROP_KEY.CommonStyle, PROP_KEY.Gap),
        formItemProps: {
          label: t("Kho\u1EA3ng c\xE1ch")
        },
        sliderProps: {
          min: 2,
          max: 30
        },
        unitProps: { defaultValue: UnitEnum.pixel }
      })
    ]
  });
};

export { StyleSetting };
