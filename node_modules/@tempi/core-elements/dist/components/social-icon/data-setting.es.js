import { jsxs, Fragment, jsx } from 'react/jsx-runtime';
import { t } from '../../i18n.es.js';
import { PlusOutlined } from '@ant-design/icons';
import { useProp } from '@tempi/core-editor';
import { Button } from 'antd';
import { useState } from 'react';
import { DEFAULT_ICON } from './constants.es.js';
import { SocialItemSetting } from './SocialItemSetting.es.js';

const DataSetting = () => {
  const [icons, setIcons] = useProp("icons");
  const [currentExpandIndex, setCurrentExpandIndex] = useState(0);
  const onRemove = (index) => {
    const newIcons = icons.filter((_, _index) => _index !== index);
    setIcons(newIcons);
  };
  const onAddIcon = () => {
    setIcons([...icons, DEFAULT_ICON]);
  };
  return /* @__PURE__ */ jsxs(Fragment, {
    children: [
      icons.map((e, index) => {
        return /* @__PURE__ */ jsx(SocialItemSetting, {
          index,
          icon: e,
          onRemove,
          currentExpandIndex,
          setExpandIndex: setCurrentExpandIndex
        }, index);
      }),
      /* @__PURE__ */ jsx(Button, {
        style: {
          marginTop: 8,
          marginBottom: 8
        },
        type: "primary",
        block: true,
        icon: /* @__PURE__ */ jsx(PlusOutlined, {}),
        onClick: onAddIcon,
        children: t("Th\xEAm icon")
      })
    ]
  });
};

export { DataSetting };
