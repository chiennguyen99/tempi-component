import { jsx } from 'react/jsx-runtime';
import styled from '@emotion/styled';
import { Icon } from '@tempi/icons';
import { getStyle } from '@tempi/core-renderer';
import { Link } from '../../core/Link/index.es.js';

var __defProp = Object.defineProperty;
var __defProps = Object.defineProperties;
var __getOwnPropDescs = Object.getOwnPropertyDescriptors;
var __getOwnPropSymbols = Object.getOwnPropertySymbols;
var __hasOwnProp = Object.prototype.hasOwnProperty;
var __propIsEnum = Object.prototype.propertyIsEnumerable;
var __defNormalProp = (obj, key, value) => key in obj ? __defProp(obj, key, { enumerable: true, configurable: true, writable: true, value }) : obj[key] = value;
var __spreadValues = (a, b) => {
  for (var prop in b || (b = {}))
    if (__hasOwnProp.call(b, prop))
      __defNormalProp(a, prop, b[prop]);
  if (__getOwnPropSymbols)
    for (var prop of __getOwnPropSymbols(b)) {
      if (__propIsEnum.call(b, prop))
        __defNormalProp(a, prop, b[prop]);
    }
  return a;
};
var __spreadProps = (a, b) => __defProps(a, __getOwnPropDescs(b));
const socialIconMapping = {
  facebook: {
    icon: "FacebookFilled",
    color: "#415993"
  },
  youtube: {
    icon: "YoutubeFilled",
    color: "#bd342b"
  },
  tiktok: {
    icon: "TiktokFilled",
    color: "#262626"
  },
  instagram: {
    icon: "InstagramFilled",
    color: "#d96357"
  },
  twitter: {
    icon: "TwitterFilled",
    color: "#1DA1F2"
  },
  github: {
    icon: "GithubFilled",
    color: "#000000"
  },
  zalo: { icon: "ZaloFilled", color: "#2d75f6" }
};
const SocialIcon = ({
  commonStyle,
  icons,
  iconSize,
  iconStyle
}) => {
  return /* @__PURE__ */ jsx(Wrapper, {
    commonStyle,
    children: icons.map((e, index) => {
      var _a, _b;
      if (!socialIconMapping[e.name])
        return;
      return /* @__PURE__ */ jsx(Link, __spreadProps(__spreadValues({}, e.link), {
        children: /* @__PURE__ */ jsx(IconWrapper, {
          iconStyle: __spreadProps(__spreadValues({}, iconStyle), {
            height: iconStyle.width,
            backgroundColor: (_a = socialIconMapping[e.name]) == null ? void 0 : _a.color
          }),
          children: /* @__PURE__ */ jsx(Icon, {
            name: (_b = socialIconMapping == null ? void 0 : socialIconMapping[e.name]) == null ? void 0 : _b.icon,
            style: { fontSize: iconSize || 25 }
          })
        })
      }), index);
    })
  });
};
const Wrapper = styled.div(
  ({ commonStyle }) => `
  ${getStyle(commonStyle)};
  display: flex;
`
);
const IconWrapper = styled.div(
  ({ iconStyle }) => `
${getStyle(iconStyle)};
display: flex;
align-items: center;
justify-content: center;
border-radius: 10%;
> span {
  color: white;
  line-height: 1; 
}
`
);
SocialIcon.displayName = "SocialIcon";

export { SocialIcon };
