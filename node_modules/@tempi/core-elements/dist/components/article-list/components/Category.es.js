import { jsx } from 'react/jsx-runtime';
import styled from '@emotion/styled';
import { generate } from '@tempi/core-renderer';

const Category = ({
  category,
  type,
  color
}) => {
  if (!category)
    return null;
  const Wrapper = type === "button" ? ButtonCategory : TextCategory;
  return /* @__PURE__ */ jsx(Wrapper, {
    $color: color,
    children: category.name
  });
};
const ButtonCategory = styled.span`
  color: ${({ $color }) => $color};
  background-color: ${({ $color }) => generate($color)["100"]};
  padding: 2px 8px;
  border-radius: 1em;
`;
const TextCategory = styled.span`
  color: ${({ $color }) => $color};

  &:after {
    content: '•';
    margin-left: 0.5em;
  }

  &:last-child:after {
    content: none;
  }
`;

export { Category };
