import { AlignmentType, ResponsiveResource, Style } from '@tempi/core-renderer';
import { ButtonV2Interface } from '../button';
import { ConfigStorageInterface } from '../form';
import { ImageInterface } from '../image';
import { SliderInterface } from '../slider';
export type ProductListInterface = ResponsiveResource<ProductListBaseInterface>;
export type ProductListBaseInterface = (GridInterface | CarouselInterface) & {
    eventType: ProductListEventType;
    configStorage?: ConfigStorageInterface;
    items: ProductItemInterface[];
    alignment: AlignmentType;
    nameStyle?: Style;
    priceStyle?: Style;
    currencyUnit?: string;
    currencyAlignment?: 'left' | 'right';
    orderButton?: ButtonV2Interface;
    buttonLink?: string;
    showButton?: boolean;
};
export interface ProductItemInterface {
    image?: ImageInterface;
    name: string;
    price?: number;
    discount?: number;
    link?: string;
    virtualSold?: number;
    virtualRating?: number;
}
export type ProductListEventType = 'showPopup' | 'link';
export interface CarouselInterface {
    layout: 'carousel';
    carouselOptions: Omit<SliderInterface, 'items'>;
}
export interface GridInterface {
    layout: 'grid';
    gridOptions: GridOptionInterface;
}
export interface GridOptionInterface {
    rows: number;
    columns: number;
}
//# sourceMappingURL=type.d.ts.map