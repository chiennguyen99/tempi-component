import React from 'react';
interface CheckoutPopupProps<T extends {}> {
    open: boolean;
    setOpen: React.Dispatch<React.SetStateAction<boolean>>;
    loading: boolean;
    onFinish: (values: T) => any;
}
export declare const CheckoutPopup: <T extends {}>({ open, setOpen, onFinish, loading, }: CheckoutPopupProps<T>) => JSX.Element;
export {};
//# sourceMappingURL=CheckoutPopup.d.ts.map