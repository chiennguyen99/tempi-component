import { jsx } from 'react/jsx-runtime';
import { useInjectDynamicLinks, getStyle } from '@tempi/core-renderer';
import styled from '@emotion/styled';
import { Link } from '../../core/Link/index.es.js';
import { getCommonTextCss } from '../../constants/fonts.es.js';

var __defProp = Object.defineProperty;
var __defProps = Object.defineProperties;
var __getOwnPropDescs = Object.getOwnPropertyDescriptors;
var __getOwnPropSymbols = Object.getOwnPropertySymbols;
var __hasOwnProp = Object.prototype.hasOwnProperty;
var __propIsEnum = Object.prototype.propertyIsEnumerable;
var __defNormalProp = (obj, key, value) => key in obj ? __defProp(obj, key, { enumerable: true, configurable: true, writable: true, value }) : obj[key] = value;
var __spreadValues = (a, b) => {
  for (var prop in b || (b = {}))
    if (__hasOwnProp.call(b, prop))
      __defNormalProp(a, prop, b[prop]);
  if (__getOwnPropSymbols)
    for (var prop of __getOwnPropSymbols(b)) {
      if (__propIsEnum.call(b, prop))
        __defNormalProp(a, prop, b[prop]);
    }
  return a;
};
var __spreadProps = (a, b) => __defProps(a, __getOwnPropDescs(b));
var __objRest = (source, exclude) => {
  var target = {};
  for (var prop in source)
    if (__hasOwnProp.call(source, prop) && exclude.indexOf(prop) < 0)
      target[prop] = source[prop];
  if (source != null && __getOwnPropSymbols)
    for (var prop of __getOwnPropSymbols(source)) {
      if (exclude.indexOf(prop) < 0 && __propIsEnum.call(source, prop))
        target[prop] = source[prop];
    }
  return target;
};
const Heading = (_a) => {
  var _b = _a, {
    text,
    enabled,
    link,
    children
  } = _b, rest = __objRest(_b, [
    "text",
    "enabled",
    "link",
    "children"
  ]);
  const { text: formattedText } = useInjectDynamicLinks(text);
  const renderHeading = () => {
    if (children)
      return /* @__PURE__ */ jsx(StyledHeading, __spreadProps(__spreadValues({}, rest), {
        children
      }));
    return /* @__PURE__ */ jsx(StyledHeading, __spreadProps(__spreadValues({}, rest), {
      dangerouslySetInnerHTML: {
        __html: formattedText
      }
    }));
  };
  const heading = renderHeading();
  if (!enabled)
    return /* @__PURE__ */ jsx(Link, __spreadProps(__spreadValues({}, link), {
      children: heading
    }));
  return heading;
};
const StyledHeading = styled.div(
  ({ commonStyle }) => `
    ${getStyle(commonStyle)}
    ${getCommonTextCss(true)}
  `,
  `a {
    color: inherit;
  }`
);
Heading.displayName = "Heading";

export { Heading, StyledHeading };
