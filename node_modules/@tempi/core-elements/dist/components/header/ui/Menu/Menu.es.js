import { jsx, jsxs, Fragment } from 'react/jsx-runtime';
import { useContext } from 'react';
import { HeaderContext } from '../../context.es.js';
import { DesktopMenu } from './DesktopMenu.es.js';
import { MobileMenu } from './MobileMenu.es.js';

const Menu = ({ children }) => {
  const { isMobile } = useContext(HeaderContext);
  if (isMobile !== void 0) {
    if (isMobile) {
      return /* @__PURE__ */ jsx(MobileMenu, {
        children
      });
    }
    return /* @__PURE__ */ jsx(DesktopMenu, {
      children
    });
  }
  return /* @__PURE__ */ jsxs(Fragment, {
    children: [
      /* @__PURE__ */ jsx(MobileMenu, {
        children
      }),
      /* @__PURE__ */ jsx(DesktopMenu, {
        children
      })
    ]
  });
};
Menu.displayName = "Menu";

export { Menu };
