import { jsxs, Fragment, jsx } from 'react/jsx-runtime';
import { t } from '../../../../i18n.es.js';
import { ColorPickerConfig, InputNumberConfig } from '@tempi/core-editor';
import { UnitEnum } from '@tempi/core-renderer';
import { PROP_KEY } from '../../../../constants/propKey.es.js';

const ToggleIconSetting = () => {
  const srcPropKey = PROP_KEY.joinKey(PROP_KEY.Toggle, PROP_KEY.Icon);
  return /* @__PURE__ */ jsxs(Fragment, {
    children: [
      /* @__PURE__ */ jsx(ColorPickerConfig, {
        propKey: PROP_KEY.joinKey(srcPropKey, PROP_KEY.Color),
        placeholder: t("Ch\u1ECDn m\xE0u"),
        formItemProps: {
          label: t("M\xE0u bi\u1EC3u t\u01B0\u1EE3ng")
        }
      }),
      /* @__PURE__ */ jsx(InputNumberConfig, {
        propKey: PROP_KEY.joinKey(srcPropKey, PROP_KEY.Size),
        formItemProps: {
          label: t("K\xEDch th\u01B0\u1EDBc bi\u1EC3u t\u01B0\u1EE3ng")
        },
        unitProps: { defaultValue: UnitEnum.pixel }
      })
    ]
  });
};

export { ToggleIconSetting };
