import { withSelectorDirectElement, AdvanceAttributes } from '@tempi/core-editor';
import React__default from 'react';
import { ImageQuickSetting } from '../../../image/quick-setting.es.js';
import { ImageSettings } from '../../../image/setting.es.js';
import { DEFAULT_HEADER } from '../../constants.es.js';
import { Logo } from '../../ui/Logo/Logo.es.js';

const LogoEditor = withSelectorDirectElement(Logo, {
  displayName: Logo.displayName,
  tag: Logo.displayName.toLowerCase(),
  advanceAttributes: AdvanceAttributes,
  quickSetting: ImageQuickSetting,
  customAttributes: () => React__default.createElement(ImageSettings, {
    showZoomPreview: false,
    showAlignment: false
  }),
  props: DEFAULT_HEADER.logo,
  rules: {
    canDrag: () => false
  }
});

export { LogoEditor };
