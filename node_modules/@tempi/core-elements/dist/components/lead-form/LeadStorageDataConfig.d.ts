import { DefaultOptionType } from 'antd/es/select';
import { APIConfig, EmailConfig, FormConfig, GoogleSheetConfig } from '../../configs';
interface StorageDataConfigProps {
    handleFetchFormAccount: (cb?: (formAccounts: {
        label: string;
        value: number;
    }[]) => void) => Promise<void>;
    loading: boolean;
    configStorage: FormConfig;
    handleSaveDataConfig: (configs: (EmailConfig & GoogleSheetConfig & APIConfig)[]) => Promise<void>;
    ggSheetsAccounts: DefaultOptionType[];
}
export declare const LeadStorageDataConfig: ({ handleFetchFormAccount, loading, configStorage, handleSaveDataConfig, ggSheetsAccounts, }: StorageDataConfigProps) => JSX.Element;
export {};
//# sourceMappingURL=LeadStorageDataConfig.d.ts.map