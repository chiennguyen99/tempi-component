import { jsx } from 'react/jsx-runtime';
import { useProp } from '@tempi/core-editor';
import { BLOCK_TYPES } from './type.es.js';
import { HeaderV2Setting } from './header-v2/setting.es.js';
import { RowSetting } from '../row/setting.es.js';
import { PROP_KEY } from '../../constants/propKey.es.js';

const GlobalBlockSetting = () => {
  const [blockType] = useProp(PROP_KEY.BlockType);
  return blockType === BLOCK_TYPES.HEADER ? /* @__PURE__ */ jsx(HeaderV2Setting, {}) : /* @__PURE__ */ jsx(RowSetting, {});
};

export { GlobalBlockSetting };
