import { jsx } from 'react/jsx-runtime';
import { useProp, AdvanceAttributes } from '@tempi/core-editor';
import { BLOCK_TYPES } from './type.es.js';
import { PROP_KEY } from '../../constants/propKey.es.js';

const GlobalBlockAdvancedSetting = () => {
  const [blockType] = useProp(PROP_KEY.BlockType);
  if (blockType === BLOCK_TYPES.HEADER)
    return null;
  return /* @__PURE__ */ jsx(AdvanceAttributes, {});
};

export { GlobalBlockAdvancedSetting };
