import { jsx } from 'react/jsx-runtime';
import { useProp } from '@tempi/core-editor';
import { BLOCK_TYPES } from './type.es.js';
import { HeaderV2StyleSetting } from './header-v2/style-setting.es.js';
import { RowStyle } from '../row/style-setting.es.js';
import { PROP_KEY } from '../../constants/propKey.es.js';

const GlobalBlockStyleSetting = () => {
  const [blockType] = useProp(PROP_KEY.BlockType);
  return blockType === BLOCK_TYPES.HEADER ? /* @__PURE__ */ jsx(HeaderV2StyleSetting, {}) : /* @__PURE__ */ jsx(RowStyle, {});
};

export { GlobalBlockStyleSetting };
