import { jsxs, Fragment, jsx } from 'react/jsx-runtime';
import { t } from '../../../i18n.es.js';
import { useBuilder, QuickSettingButton, OpenSettingDialog, DeleteNode } from '@tempi/core-editor';

const HeaderV2QuickSetting = () => {
  const { setOpenNavigation } = useBuilder();
  return /* @__PURE__ */ jsxs(Fragment, {
    children: [
      /* @__PURE__ */ jsx(QuickSettingButton, {
        onClick: (e) => {
          e.stopPropagation();
          setOpenNavigation == null ? void 0 : setOpenNavigation(true);
        },
        children: t("Ch\u1EC9nh s\u1EEDa \u0111i\u1EC1u h\u01B0\u1EDBng")
      }),
      /* @__PURE__ */ jsx(OpenSettingDialog, {}),
      /* @__PURE__ */ jsx(DeleteNode, {})
    ]
  });
};

export { HeaderV2QuickSetting };
