'use strict';

Object.defineProperty(exports, '__esModule', { value: true });

var jsxRuntime = require('react/jsx-runtime');
var coreEditor = require('@tempi/core-editor');
var React = require('react');
var propKey = require('../../constants/propKey.js');

const SearchSetting = () => {
  coreEditor.usePropSeparateDevice(
    propKey.PROP_KEY.joinKey(propKey.PROP_KEY.SearchConfig, propKey.PROP_KEY.Show)
  );
  const [icon, setIcon] = coreEditor.usePropSeparateDevice(
    propKey.PROP_KEY.joinKey(propKey.PROP_KEY.SearchConfig, propKey.PROP_KEY.Icon)
  );
  React.useEffect(() => {
    if (!icon) {
      setIcon({
        color: "#000000",
        size: 24,
        name: "SearchOutlined"
      });
    }
  }, []);
  return /* @__PURE__ */ jsxRuntime.jsx("div", {
    style: { marginTop: 16 },
    children: false
  });
};

exports.SearchSetting = SearchSetting;
