import { jsx } from 'react/jsx-runtime';
import { usePropSeparateDevice } from '@tempi/core-editor';
import { useEffect } from 'react';
import { PROP_KEY } from '../../constants/propKey.es.js';

const SearchSetting = () => {
  usePropSeparateDevice(
    PROP_KEY.joinKey(PROP_KEY.SearchConfig, PROP_KEY.Show)
  );
  const [icon, setIcon] = usePropSeparateDevice(
    PROP_KEY.joinKey(PROP_KEY.SearchConfig, PROP_KEY.Icon)
  );
  useEffect(() => {
    if (!icon) {
      setIcon({
        color: "#000000",
        size: 24,
        name: "SearchOutlined"
      });
    }
  }, []);
  return /* @__PURE__ */ jsx("div", {
    style: { marginTop: 16 },
    children: false
  });
};

export { SearchSetting };
