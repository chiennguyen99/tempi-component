import { jsx, Fragment } from 'react/jsx-runtime';
import { t } from '../../i18n.es.js';
import { RangeSliderConfig } from '@tempi/core-editor';
import { PROP_KEY } from '../../constants/propKey.es.js';

const ArticleHeaderSetting = () => {
  return /* @__PURE__ */ jsx(Fragment, {
    children: /* @__PURE__ */ jsx(RangeSliderConfig, {
      propKey: PROP_KEY.joinKey(PROP_KEY.CommonStyle, PROP_KEY.MaxWidth),
      formItemProps: {
        label: t("Chi\u1EC1u r\u1ED9ng n\u1ED9i dung"),
        layout: "vertical"
      },
      unitProps: {
        defaultValue: "px"
      },
      sliderProps: {
        min: 500,
        max: 2e3
      }
    })
  });
};

export { ArticleHeaderSetting };
