import { jsxs, Fragment, jsx } from 'react/jsx-runtime';
import { t } from '../../../i18n.es.js';
import { InputNumberConfig, InputConfig } from '@tempi/core-editor';
import { UnitEnum } from '@tempi/core-renderer';
import { PROP_KEY } from '../../../constants/propKey.es.js';

const ConfigInlineBanner = () => {
  return /* @__PURE__ */ jsxs(Fragment, {
    children: [
      /* @__PURE__ */ jsx(InputNumberConfig, {
        propKey: PROP_KEY.joinKey(PROP_KEY.CommonStyle, PROP_KEY.Height),
        formItemProps: {
          label: t("Chi\u1EC1u cao")
        },
        unitProps: { defaultValue: UnitEnum.pixel }
      }),
      /* @__PURE__ */ jsx(InputNumberConfig, {
        propKey: PROP_KEY.joinKey(PROP_KEY.CommonStyle, PROP_KEY.Width),
        formItemProps: {
          label: t("Chi\u1EC1u d\xE0i")
        },
        unitProps: { defaultValue: UnitEnum.pixel }
      }),
      /* @__PURE__ */ jsx(InputConfig, {
        propKey: PROP_KEY.CssSelector,
        formItemProps: {
          label: "Css selector"
        }
      }),
      /* @__PURE__ */ jsx(InputConfig, {
        propKey: PROP_KEY.CustomCss,
        formItemProps: {
          label: "Custom CSS"
        }
      })
    ]
  });
};

export { ConfigInlineBanner, ConfigInlineBanner as default };
