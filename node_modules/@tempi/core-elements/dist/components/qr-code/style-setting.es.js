import { jsxs, Fragment, jsx } from 'react/jsx-runtime';
import { t } from '../../i18n.es.js';
import { ColorPickerConfig } from '@tempi/core-editor';
import { PROP_KEY } from '../../constants/propKey.es.js';

const StyleSetting = () => {
  return /* @__PURE__ */ jsxs(Fragment, {
    children: [
      /* @__PURE__ */ jsx(ColorPickerConfig, {
        formItemProps: { label: t("M\xE0u QR Code") },
        propKey: PROP_KEY.joinKey(PROP_KEY.QRCodeStyle, PROP_KEY.Color)
      }),
      /* @__PURE__ */ jsx(ColorPickerConfig, {
        formItemProps: { label: t("M\xE0u n\u1EC1n QR Code") },
        propKey: PROP_KEY.joinKey(
          PROP_KEY.QRCodeStyle,
          PROP_KEY.BackgroundColor
        )
      })
    ]
  });
};

export { StyleSetting };
