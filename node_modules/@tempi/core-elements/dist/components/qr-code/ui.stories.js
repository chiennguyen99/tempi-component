import { jsx as _jsx } from "react/jsx-runtime";
import { Alignment } from '@tempi/core-renderer';
import { QRCode } from './ui';
const defaultQRCode = {
    value: 'https://tempi.vn/',
    alignment: Alignment.center,
    bordered: true,
    size: 200,
};
export default {
    title: 'CoreElements/QRCode',
};
const Template = (args) => {
    return (_jsx("div", { children: _jsx(QRCode, Object.assign({}, args)) }));
};
export const Default = Template.bind({});
Default.args = defaultQRCode;
