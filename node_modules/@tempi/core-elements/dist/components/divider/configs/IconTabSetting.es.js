import { jsxs, Fragment, jsx } from 'react/jsx-runtime';
import { t } from '../../../i18n.es.js';
import { useProp, AlignmentConfig, FormItem } from '@tempi/core-editor';
import { UnitEnum } from '@tempi/core-renderer';
import { InputNumber } from 'antd';
import { PROP_KEY } from '../../../constants/propKey.es.js';
import { IconSettings } from '../../../configs/IconSettings/IconSettings.es.js';

const IconTabSetting = ({ propKey }) => {
  const [iconSpacing, setIconSpacing] = useProp(
    PROP_KEY.joinKey(propKey, PROP_KEY.IconConfig, PROP_KEY.Spacing)
  );
  return /* @__PURE__ */ jsxs(Fragment, {
    children: [
      /* @__PURE__ */ jsx(IconSettings, {
        propKey: PROP_KEY.joinKey(propKey, PROP_KEY.IconConfig, PROP_KEY.Icon)
      }),
      /* @__PURE__ */ jsx(AlignmentConfig, {
        propKey: PROP_KEY.joinKey(propKey, PROP_KEY.IconConfig, PROP_KEY.Align),
        formItemProps: {
          label: t("V\u1ECB tr\xED Icon")
        }
      }),
      /* @__PURE__ */ jsx(FormItem, {
        label: t("Kho\u1EA3ng c\xE1ch"),
        children: /* @__PURE__ */ jsx(InputNumber, {
          value: iconSpacing == null ? void 0 : iconSpacing.size,
          onChange: (val) => {
            setIconSpacing({
              unit: UnitEnum.pixel,
              size: val
            });
          },
          addonAfter: "px",
          min: 0,
          style: { width: 120 }
        })
      })
    ]
  });
};

export { IconTabSetting };
