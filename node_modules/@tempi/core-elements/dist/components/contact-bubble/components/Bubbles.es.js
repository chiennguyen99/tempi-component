import { jsx, Fragment } from 'react/jsx-runtime';
import styled from '@emotion/styled';
import { BubbleItem } from './BubbleItem.es.js';
import { FlashingBubbleItems } from './FlashingBubbleItems.es.js';
import { ContactBubbleLayout } from '../type.es.js';

const Bubbles = ({ items, layout, position, isEditorMode }) => {
  const filterItems = items.filter((item) => {
    var _a;
    return !!((_a = item == null ? void 0 : item.icon) == null ? void 0 : _a.src);
  });
  switch (layout) {
    case ContactBubbleLayout.VERTICAL:
      return /* @__PURE__ */ jsx(VerticalBubbles, {
        children: filterItems.map((item, index) => /* @__PURE__ */ jsx(BubbleItem, {
          item,
          position,
          isEditorMode
        }, index))
      });
    case ContactBubbleLayout.FLASH:
      return /* @__PURE__ */ jsx(FlashingBubbleItems, {
        items: filterItems,
        position,
        isEditorMode
      });
    default:
      return /* @__PURE__ */ jsx(Fragment, {});
  }
};
const VerticalBubbles = styled.div`
  display: flex;
  flex-direction: column;
  gap: 1em;
`;

export { Bubbles };
