import styled from '@emotion/styled';

const Masonry = styled.div(
  ({ columnCount, gap }) => `
    display: grid;
    grid-template-columns: repeat(${columnCount}, auto);
    grid-column-gap: ${gap}px;
    height: calc(100% + ${gap}px);
    margin-bottom: ${-gap}px;
  `
);
const MasonryImageWrapper = styled.div(
  ({ gap }) => `
  margin-bottom: ${gap}px;
  display: flex;
  `
);

export { Masonry, MasonryImageWrapper };
