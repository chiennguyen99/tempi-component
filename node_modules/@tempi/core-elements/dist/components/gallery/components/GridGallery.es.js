import styled from '@emotion/styled';

const Grid = styled.div(
  ({ columnCount, gap }) => `
    display: grid;
    grid-template-columns: repeat(${columnCount}, 1fr);
    grid-gap: ${gap}px;
  `
);
const Block = styled.div`
  position: relative;
  padding-top: 100%;
  overflow: hidden;
`;
const ImageWrapper = styled.div`
  position: absolute;
  top: 0;
  left: 0;
  display: block;
  width: 100%;
  height: 100%;
`;

export { Block, Grid, ImageWrapper };
