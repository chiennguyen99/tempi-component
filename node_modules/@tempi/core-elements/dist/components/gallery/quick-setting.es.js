import { jsxs, Fragment, jsx } from 'react/jsx-runtime';
import { ImageListQuickSetting, DuplicateNode, OpenSettingDialog, DeleteNode } from '@tempi/core-editor';
import { PROP_KEY } from '../../constants/propKey.es.js';

const GalleryQuickSetting = () => {
  return /* @__PURE__ */ jsxs(Fragment, {
    children: [
      /* @__PURE__ */ jsx(ImageListQuickSetting, {
        propKey: PROP_KEY.Images
      }),
      /* @__PURE__ */ jsx(DuplicateNode, {}),
      /* @__PURE__ */ jsx(OpenSettingDialog, {}),
      /* @__PURE__ */ jsx(DeleteNode, {})
    ]
  });
};

export { GalleryQuickSetting };
