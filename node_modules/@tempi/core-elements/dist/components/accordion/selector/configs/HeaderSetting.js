import { jsx as _jsx, jsxs as _jsxs, Fragment as _Fragment } from "react/jsx-runtime";
import { t } from '../../../../i18n';
import { ColorPickerConfig, InputNumberConfig, useProp, AlignmentConfig, } from '@tempi/core-editor';
import { Alignment, UnitEnum } from '@tempi/core-renderer';
import { Space } from 'antd';
import { PROP_KEY } from '../../../../constants';
export const HeaderSetting = () => {
    const iconPostionKey = PROP_KEY.joinKey(PROP_KEY.Icon, PROP_KEY.Align);
    const [iconPosition] = useProp(iconPostionKey);
    return (_jsx(_Fragment, { children: _jsxs(Space, Object.assign({ style: { padding: '8px 12px' } }, { children: [_jsx(InputNumberConfig, { formItemProps: {
                        label: t('Chiều cao'),
                    }, unitProps: { defaultValue: UnitEnum.pixel }, propKey: PROP_KEY.joinKey(PROP_KEY.HeaderStyle, 'height') }), _jsx(ColorPickerConfig, { formItemProps: {
                        label: t('Màu nhãn'),
                    }, propKey: PROP_KEY.joinKey(PROP_KEY.TitleStyle, PROP_KEY.Color) }), _jsx(ColorPickerConfig, { formItemProps: {
                        label: t('Màu biểu tượng'),
                    }, propKey: PROP_KEY.joinKey(PROP_KEY.Icon, PROP_KEY.Color) }), _jsx(AlignmentConfig, { formItemProps: {
                        label: t('Vị trí biểu tượng'),
                    }, propKey: iconPostionKey, buttonGroupProps: {
                        listAlignment: ['left', 'right'],
                    } }), iconPosition === Alignment.left && (_jsx(InputNumberConfig, { propKey: PROP_KEY.joinKey(PROP_KEY.HeaderStyle, PROP_KEY.ColumnGap), formItemProps: {
                        label: t('Khoảng cách tiêu đề và biểu tượng'),
                    }, inputNumberProps: {
                        placeholder: '0',
                    }, unitProps: { defaultValue: UnitEnum.pixel } }))] })) }));
};
