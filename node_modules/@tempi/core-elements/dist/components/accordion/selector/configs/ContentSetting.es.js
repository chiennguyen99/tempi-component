import { jsxs, Fragment, jsx } from 'react/jsx-runtime';
import { t } from '../../../../i18n.es.js';
import { AlignmentConfig, SpacingAttribute } from '@tempi/core-editor';
import { PROP_KEY } from '../../../../constants/propKey.es.js';

const ContentSetting = () => {
  return /* @__PURE__ */ jsxs(Fragment, {
    children: [
      /* @__PURE__ */ jsx(AlignmentConfig, {
        formItemProps: {
          label: t("C\u0103n ch\u1EC9nh")
        },
        propKey: PROP_KEY.joinKey(PROP_KEY.ContentAlign),
        buttonGroupProps: {
          listAlignment: ["left", "center", "right"]
        }
      }),
      /* @__PURE__ */ jsx(SpacingAttribute, {
        title: "Padding",
        propKey: PROP_KEY.joinKey(PROP_KEY.ContentStyle, PROP_KEY.Padding)
      })
    ]
  });
};

export { ContentSetting };
