import { jsx } from 'react/jsx-runtime';
import { TextCaseUppercaseRegular } from '@fluentui/react-icons';
import { QuickSettingButton } from '@tempi/core-editor';
import { getEditorAttributeValue } from '../../utils/getEditorAttributeValue.es.js';

const TextTransformQuickSetting = ({
  editor
}) => {
  const isActive = getEditorAttributeValue(editor, "textTransform") === "uppercase" /* uppercase */;
  const setTextTransform = (value) => {
    var _a;
    (_a = editor == null ? void 0 : editor.commands) == null ? void 0 : _a.executeCmd(
      ({ commands }) => commands.setTextTransform(value)
    );
  };
  const onClick = () => {
    setTextTransform(isActive ? null : "uppercase" /* uppercase */);
  };
  return /* @__PURE__ */ jsx(QuickSettingButton, {
    tooltip: "In hoa",
    isActive,
    onClick,
    children: /* @__PURE__ */ jsx(TextCaseUppercaseRegular, {})
  });
};

export { TextTransformQuickSetting };
