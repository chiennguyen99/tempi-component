import { jsx } from 'react/jsx-runtime';
import { TextBoldRegular } from '@fluentui/react-icons';
import { QuickSettingButton } from '@tempi/core-editor';

const BoldQuickSetting = ({ editor }) => {
  return /* @__PURE__ */ jsx(QuickSettingButton, {
    isActive: editor == null ? void 0 : editor.isActive("bold"),
    onClick: () => {
      var _a, _b;
      (_b = (_a = editor == null ? void 0 : editor.commands) == null ? void 0 : _a.executeCmd) == null ? void 0 : _b.call(_a, ({ commands }) => commands.toggleBold());
    },
    children: /* @__PURE__ */ jsx(TextBoldRegular, {})
  });
};

export { BoldQuickSetting };
