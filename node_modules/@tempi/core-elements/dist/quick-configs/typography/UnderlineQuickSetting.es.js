import { jsx } from 'react/jsx-runtime';
import { UnderlineOutlined } from '@ant-design/icons';
import { QuickSettingButton } from '@tempi/core-editor';

const UnderlineQuickSetting = ({
  editor
}) => {
  var _a;
  const onClick = () => {
    var _a2;
    (_a2 = editor == null ? void 0 : editor.commands) == null ? void 0 : _a2.executeCmd(({ commands }) => commands.toggleUnderline());
  };
  return /* @__PURE__ */ jsx(QuickSettingButton, {
    isActive: (_a = editor == null ? void 0 : editor.isActive) == null ? void 0 : _a.call(editor, "underline"),
    onClick,
    children: /* @__PURE__ */ jsx(UnderlineOutlined, {})
  });
};

export { UnderlineQuickSetting };
