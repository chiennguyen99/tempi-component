import { jsx } from 'react/jsx-runtime';
import { TextItalicRegular } from '@fluentui/react-icons';
import { QuickSettingButton } from '@tempi/core-editor';

const ItalicQuickSetting = ({
  editor
}) => {
  return /* @__PURE__ */ jsx(QuickSettingButton, {
    isActive: editor == null ? void 0 : editor.isActive("italic"),
    onClick: () => {
      var _a, _b;
      (_b = (_a = editor == null ? void 0 : editor.commands) == null ? void 0 : _a.executeCmd) == null ? void 0 : _b.call(
        _a,
        ({ commands }) => commands.toggleItalic()
      );
    },
    children: /* @__PURE__ */ jsx(TextItalicRegular, {})
  });
};

export { ItalicQuickSetting };
