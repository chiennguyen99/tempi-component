import { jsx } from 'react/jsx-runtime';
import { t } from '../../i18n.es.js';
import { EyeRegular } from '@fluentui/react-icons';
import { useBuilder, SubQuickSetting, ResponsiveAttribute, QuickSettingButton } from '@tempi/core-editor';

const VisibleOnDevice = () => {
  const { allowConfigResponsive } = useBuilder();
  if (!allowConfigResponsive)
    return null;
  return /* @__PURE__ */ jsx(SubQuickSetting, {
    content: /* @__PURE__ */ jsx(ResponsiveAttribute, {}),
    children: /* @__PURE__ */ jsx(QuickSettingButton, {
      tooltip: t("\u1EA8n/Hi\u1EC7n tr\xEAn thi\u1EBFt b\u1ECB"),
      children: /* @__PURE__ */ jsx(EyeRegular, {
        style: { fontSize: 16 }
      })
    })
  });
};

export { VisibleOnDevice };
