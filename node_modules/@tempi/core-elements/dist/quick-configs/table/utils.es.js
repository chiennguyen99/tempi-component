const isToggledHeaderRow = (editor) => {
  var _a, _b, _c, _d, _e;
  if (!editor)
    return false;
  const jsonEditor = (_a = editor.getJSON) == null ? void 0 : _a.call(editor);
  if (jsonEditor.type !== "doc" && ((_b = jsonEditor.content) == null ? void 0 : _b[0].type) !== "table")
    return false;
  const table = jsonEditor.content[0];
  const firstRow = (_c = table.content) == null ? void 0 : _c[0];
  if (!firstRow || !firstRow.content)
    return false;
  return (_e = (_d = firstRow.content) == null ? void 0 : _d.every) == null ? void 0 : _e.call(
    _d,
    (cell) => cell.type === "tableHeader"
  );
};
const isToggledHeaderColumn = (editor) => {
  var _a, _b, _c, _d;
  if (!editor)
    return false;
  const jsonEditor = (_a = editor.getJSON) == null ? void 0 : _a.call(editor);
  if (jsonEditor.type !== "doc" && ((_b = jsonEditor.content) == null ? void 0 : _b[0].type) !== "table")
    return false;
  const table = jsonEditor.content[0];
  return (_d = (_c = table.content) == null ? void 0 : _c.every) == null ? void 0 : _d.call(
    _c,
    (row) => {
      var _a2;
      return ((_a2 = row.content) == null ? void 0 : _a2[0].type) === "tableHeader";
    }
  );
};

export { isToggledHeaderColumn, isToggledHeaderRow };
