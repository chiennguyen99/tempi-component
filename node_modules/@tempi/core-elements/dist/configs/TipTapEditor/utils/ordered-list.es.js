import BaseOrderedList from '@tiptap/extension-ordered-list';
import { moveStyleToListItem } from './common.es.js';

const OrderedList = BaseOrderedList.extend({
  renderHTML({ HTMLAttributes, node }) {
    var _a;
    moveStyleToListItem(node, HTMLAttributes);
    return (_a = this.parent) == null ? void 0 : _a.call(this, { node, HTMLAttributes });
  }
});

export { OrderedList };
