'use strict';

Object.defineProperty(exports, '__esModule', { value: true });

var coreEditor = require('@tempi/core-editor');
var history = require('@tiptap/pm/history');
var react = require('@tiptap/react');
var React = require('react');
var constants = require('./constants.js');

var __defProp = Object.defineProperty;
var __defProps = Object.defineProperties;
var __getOwnPropDescs = Object.getOwnPropertyDescriptors;
var __getOwnPropSymbols = Object.getOwnPropertySymbols;
var __hasOwnProp = Object.prototype.hasOwnProperty;
var __propIsEnum = Object.prototype.propertyIsEnumerable;
var __defNormalProp = (obj, key, value) => key in obj ? __defProp(obj, key, { enumerable: true, configurable: true, writable: true, value }) : obj[key] = value;
var __spreadValues = (a, b) => {
  for (var prop in b || (b = {}))
    if (__hasOwnProp.call(b, prop))
      __defNormalProp(a, prop, b[prop]);
  if (__getOwnPropSymbols)
    for (var prop of __getOwnPropSymbols(b)) {
      if (__propIsEnum.call(b, prop))
        __defNormalProp(a, prop, b[prop]);
    }
  return a;
};
var __spreadProps = (a, b) => __defProps(a, __getOwnPropDescs(b));
const useCustomTiptap = (configs, propKey) => {
  const { setIsEditingText } = coreEditor.useBuilder();
  const [text, setText] = coreEditor.useProp(propKey);
  const { isActiveNode } = coreEditor.useNode((node) => ({
    isActiveNode: node.events.selected
  }));
  const { isActive: customIsActive, editorOptions } = configs;
  const isActive = customIsActive === void 0 ? isActiveNode : customIsActive;
  const editor = react.useEditor(__spreadProps(__spreadValues({}, editorOptions), {
    content: text,
    editable: false
  }));
  const [isEditable, setIsEditable] = React.useState(false);
  const editingTextRef = React.useRef();
  const savedTextRef = React.useRef(text);
  React.useEffect(() => {
    if (!editor)
      return;
    editor.on("update", ({ editor: editor2 }) => {
      editingTextRef.current = editor2.getHTML();
    });
  }, [editor]);
  React.useEffect(() => {
    if (!isActive) {
      setIsEditable(false);
    }
  }, [isActive]);
  React.useEffect(() => {
    if (!editor)
      return;
    const { isEditable: isEditable2, isFocused, commands } = editor;
    if (isEditable2 && !isFocused) {
      commands.focus("end");
    }
  }, [editor == null ? void 0 : editor.isEditable]);
  React.useEffect(() => {
    if (!editor)
      return;
    editor.setEditable(isEditable);
    setIsEditingText(isEditable);
    if (isEditable) {
      const { state } = editor;
      const noHistoryEvents = history.undoDepth(state) === 0 && history.redoDepth(state) === 0;
      if (!noHistoryEvents) {
        editor.chain().setContent(editor.getHTML(), false, {
          preserveWhitespace: "full"
        }).setMeta(constants.ADD_TO_HISTORY_KEY, false).run();
      }
    }
  }, [editor, isEditable]);
  React.useEffect(() => {
    if (!editor)
      return;
    if (isEditable) {
      const handleClickOutsideBuilder = () => {
        setIsEditable(false);
        const editorText = editingTextRef.current;
        if (editorText !== savedTextRef.current) {
          setText(editorText);
        }
      };
      document.addEventListener("mousedown", handleClickOutsideBuilder);
      return () => {
        document.removeEventListener("mousedown", handleClickOutsideBuilder);
      };
    } else {
      const editorText = editor.getHTML();
      if (text !== editorText) {
        setText(editorText);
      }
    }
  }, [editor, isEditable]);
  React.useEffect(() => {
    savedTextRef.current = text;
    if (editor && !isEditable && editor.getHTML() !== text) {
      editor.chain().setContent(text, false, {
        preserveWhitespace: "full"
      }).setMeta(constants.ADD_TO_HISTORY_KEY, false).blur().run();
    }
  }, [text]);
  return { editor, isEditable, setIsEditable };
};

exports.useCustomTiptap = useCustomTiptap;
