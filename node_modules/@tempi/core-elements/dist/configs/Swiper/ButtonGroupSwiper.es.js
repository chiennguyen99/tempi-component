import { jsx } from 'react/jsx-runtime';
import styled from '@emotion/styled';
import { ButtonGroup, ButtonGroupItem } from '@tempi/core-editor';

const ButtonGroupSwiper = ({
  configValue,
  setConfigValue,
  configuration
}) => {
  const handleClickItem = (value) => {
    if (value !== configValue) {
      setConfigValue(value);
      return;
    }
    setConfigValue(null);
  };
  return /* @__PURE__ */ jsx(StyledButtonGroup, {
    children: configuration.map((el, index) => {
      return /* @__PURE__ */ jsx(StyledButtonGroupItem, {
        onClick: () => handleClickItem(el.value),
        isActive: configValue === el.value,
        className: `${configValue === el.value ? "is-active" : ""}`,
        children: el.icon
      }, index);
    })
  });
};
const StyledButtonGroup = styled(ButtonGroup)`
  justify-content: space-between;
`;
const StyledButtonGroupItem = styled(ButtonGroupItem)`
  flex: 1;
`;

export { ButtonGroupSwiper };
