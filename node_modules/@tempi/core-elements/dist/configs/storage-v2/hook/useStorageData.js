'use strict';

Object.defineProperty(exports, '__esModule', { value: true });

var i18n = require('../../../i18n.js');
var coreEditor = require('@tempi/core-editor');
var React = require('react');
var type = require('../../storage/type.js');
var propKey = require('../../../constants/propKey.js');

var __defProp = Object.defineProperty;
var __defProps = Object.defineProperties;
var __getOwnPropDescs = Object.getOwnPropertyDescriptors;
var __getOwnPropSymbols = Object.getOwnPropertySymbols;
var __hasOwnProp = Object.prototype.hasOwnProperty;
var __propIsEnum = Object.prototype.propertyIsEnumerable;
var __defNormalProp = (obj, key, value) => key in obj ? __defProp(obj, key, { enumerable: true, configurable: true, writable: true, value }) : obj[key] = value;
var __spreadValues = (a, b) => {
  for (var prop in b || (b = {}))
    if (__hasOwnProp.call(b, prop))
      __defNormalProp(a, prop, b[prop]);
  if (__getOwnPropSymbols)
    for (var prop of __getOwnPropSymbols(b)) {
      if (__propIsEnum.call(b, prop))
        __defNormalProp(a, prop, b[prop]);
    }
  return a;
};
var __spreadProps = (a, b) => __defProps(a, __getOwnPropDescs(b));
const useStorageData = (isGetFormConfigs = true) => {
  const [configStorageId, setConfigStorageId] = coreEditor.useProp(
    propKey.PROP_KEY.joinKey(propKey.PROP_KEY.ConfigStorage, propKey.PROP_KEY.FormConfigId)
  );
  const [configStorage, setConfigStorage] = React.useState();
  const { pageId, getAllFormAccounts, upsertForm, getForm } = coreEditor.useBuilder();
  const [ggSheetsAccounts, setGGSheetsAccounts] = React.useState(
    []
  );
  const [loading, setLoading] = React.useState(false);
  const allowConfigStorageData = React.useMemo(() => {
    return !!getAllFormAccounts && !!upsertForm && !!getForm;
  }, [getAllFormAccounts, getForm, upsertForm]);
  React.useEffect(() => {
    if (isGetFormConfigs) {
      getFormConfigs(configStorageId);
      handleFetchFormAccount();
    }
  }, []);
  const allowConfigResponseByCode = React.useMemo(() => {
    var _a;
    return (_a = configStorage == null ? void 0 : configStorage.configs) == null ? void 0 : _a.some(
      (item) => item.type === type.FormAccountType.API
    );
  }, [configStorage]);
  const getFormConfigs = async (configStorageId2, cb) => {
    try {
      let formConfig;
      setLoading(true);
      if (!!configStorageId2) {
        formConfig = await (getForm == null ? void 0 : getForm(configStorageId2));
      }
      setConfigStorage(formConfig);
      cb == null ? void 0 : cb(formConfig == null ? void 0 : formConfig.configs);
    } finally {
      setLoading(false);
    }
  };
  const handleFetchFormAccount = async (cb) => {
    const formAccounts = await (getAllFormAccounts == null ? void 0 : getAllFormAccounts());
    const newOpts = (formAccounts || []).filter((el) => el.type === "GOOGLE_SHEET").map((el) => ({
      label: el.name,
      value: el.id
    }));
    if (cb)
      cb(newOpts);
    setGGSheetsAccounts([
      ...newOpts,
      { value: -1, label: i18n.t("Th\xEAm m\u1EDBi t\xE0i kho\u1EA3n Google") }
    ]);
  };
  const handleSaveDataConfig = async (configs) => {
    try {
      setLoading(true);
      if (configs.length) {
        let mainIndex = configs.findIndex(
          (c) => c.type === type.FormAccountType.API
        );
        if (mainIndex < 0) {
          mainIndex = configs.findIndex(
            (c) => c.type === type.FormAccountType.GOOGLE_SHEET
          );
        }
        if (mainIndex < 0) {
          mainIndex = configs.findIndex(
            (c) => c.type === type.FormAccountType.EMAIL
          );
        }
        for (const i in configs) {
          configs[i].isMainConfig = +i === mainIndex;
        }
      }
      const idUpsert = await (upsertForm == null ? void 0 : upsertForm(__spreadProps(__spreadValues({}, configStorage || {}), {
        configs,
        pageId,
        id: configStorageId
      })));
      setConfigStorageId(idUpsert);
      getFormConfigs(idUpsert);
    } finally {
      setLoading(false);
    }
  };
  return {
    configStorage,
    setConfigStorage,
    ggSheetsAccounts,
    loading,
    handleFetchFormAccount,
    getFormConfigs,
    configStorageId,
    allowConfigStorageData,
    handleSaveDataConfig,
    allowConfigResponseByCode
  };
};

exports.useStorageData = useStorageData;
