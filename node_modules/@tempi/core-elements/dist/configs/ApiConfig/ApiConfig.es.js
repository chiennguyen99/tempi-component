import { jsxs, Fragment, jsx } from 'react/jsx-runtime';
import { t } from '../../i18n.es.js';
import { EditOutlined } from '@ant-design/icons';
import styled from '@emotion/styled';
import { FormItem } from '@tempi/core-editor';
import { useState } from 'react';
import { ModalApiConfig } from './ModalApiConfig.es.js';

const ApiConfig = ({ onSubmit, data, attributes = [] }) => {
  const [isModalOpen, setIsModalOpen] = useState(false);
  return /* @__PURE__ */ jsxs(Fragment, {
    children: [
      /* @__PURE__ */ jsx(FormItem, {
        label: t("Li\xEAn k\u1EBFt API"),
        children: /* @__PURE__ */ jsxs(StyledTextButton, {
          onClick: () => setIsModalOpen(true),
          children: [
            t("Ch\u1EC9nh s\u1EEDa"),
            /* @__PURE__ */ jsx(EditOutlined, {})
          ]
        })
      }),
      /* @__PURE__ */ jsx(ModalApiConfig, {
        data,
        onSubmit,
        isModalOpen,
        closeModal: () => setIsModalOpen(false),
        attributes
      })
    ]
  });
};
const StyledTextButton = styled.div(
  ({ theme }) => `
  cursor: pointer;
  color: ${theme.colorPrimary} !important;
`
);

export { ApiConfig };
