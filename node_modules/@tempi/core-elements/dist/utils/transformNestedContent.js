'use strict';

Object.defineProperty(exports, '__esModule', { value: true });

var get = require('lodash.get');
var set = require('lodash.set');

function _interopDefaultLegacy (e) { return e && typeof e === 'object' && 'default' in e ? e : { 'default': e }; }

var get__default = /*#__PURE__*/_interopDefaultLegacy(get);
var set__default = /*#__PURE__*/_interopDefaultLegacy(set);

var __defProp = Object.defineProperty;
var __defProps = Object.defineProperties;
var __getOwnPropDescs = Object.getOwnPropertyDescriptors;
var __getOwnPropSymbols = Object.getOwnPropertySymbols;
var __hasOwnProp = Object.prototype.hasOwnProperty;
var __propIsEnum = Object.prototype.propertyIsEnumerable;
var __defNormalProp = (obj, key, value) => key in obj ? __defProp(obj, key, { enumerable: true, configurable: true, writable: true, value }) : obj[key] = value;
var __spreadValues = (a, b) => {
  for (var prop in b || (b = {}))
    if (__hasOwnProp.call(b, prop))
      __defNormalProp(a, prop, b[prop]);
  if (__getOwnPropSymbols)
    for (var prop of __getOwnPropSymbols(b)) {
      if (__propIsEnum.call(b, prop))
        __defNormalProp(a, prop, b[prop]);
    }
  return a;
};
var __spreadProps = (a, b) => __defProps(a, __getOwnPropDescs(b));
const transformNestedContent = (component, mappingKeys) => {
  if (component.tag === "tab") {
    const pathTabs = ["customAttributes", "tab", "tabs"];
    const tabs = get__default["default"](component, pathTabs);
    if (tabs == null ? void 0 : tabs.length) {
      const cloneTabs = [...tabs];
      cloneTabs.forEach((tab) => {
        tab.id = mappingKeys[tab.id] || tab.id;
      });
      set__default["default"](component, pathTabs, cloneTabs);
    }
  }
  if (component.tag === "formv4") {
    const pathSuccess = ["customAttributes", "formv4", "response", "success"];
    const pathFailed = ["customAttributes", "formv4", "response", "failed"];
    const pathCustom = ["customAttributes", "formv4", "response", "custom"];
    const pathConfigStorage = [
      "customAttributes",
      "formv4",
      "configStorage",
      "formConfigId"
    ];
    set__default["default"](component, pathConfigStorage, null);
    const successValue = get__default["default"](component, pathSuccess);
    if (successValue)
      set__default["default"](component, pathSuccess, mappingKeys[successValue] || successValue);
    const failedValue = get__default["default"](component, pathFailed);
    if (failedValue)
      set__default["default"](component, pathFailed, mappingKeys[failedValue] || failedValue);
    const customValue = get__default["default"](component, pathCustom);
    if (customValue && Array.isArray(customValue))
      set__default["default"](
        component,
        pathCustom,
        customValue.map((el) => __spreadProps(__spreadValues({}, el), {
          popup: mappingKeys[el.popup] || el.popup
        }))
      );
  }
  if (component.tag === "luckydrawv2") {
    const pathPrize = ["customAttributes", "luckydrawv2", "response", "prize"];
    const pathOutOfTurn = [
      "customAttributes",
      "luckydrawv2",
      "response",
      "outOfTurn"
    ];
    const pathOutOfPrize = [
      "customAttributes",
      "luckydrawv2",
      "response",
      "outOfPrize"
    ];
    const pathCustom = [
      "customAttributes",
      "luckydrawv2",
      "response",
      "custom"
    ];
    const pathConfigStorage = [
      "customAttributes",
      "luckydrawv2",
      "configStorage",
      "formConfigId"
    ];
    set__default["default"](component, pathConfigStorage, null);
    const prizeValue = get__default["default"](component, pathPrize);
    if (prizeValue)
      set__default["default"](component, pathPrize, mappingKeys[prizeValue] || prizeValue);
    const outOfTurnValue = get__default["default"](component, pathOutOfTurn);
    if (outOfTurnValue)
      set__default["default"](
        component,
        pathOutOfTurn,
        mappingKeys[outOfTurnValue] || outOfTurnValue
      );
    const outOfPrizeValue = get__default["default"](component, pathOutOfPrize);
    if (outOfPrizeValue)
      set__default["default"](
        component,
        pathOutOfPrize,
        mappingKeys[outOfPrizeValue] || outOfPrizeValue
      );
    const customValue = get__default["default"](component, pathCustom);
    if (customValue && Array.isArray(customValue))
      set__default["default"](
        component,
        pathCustom,
        customValue.map(
          (el) => __spreadProps(__spreadValues({}, el), {
            popup: mappingKeys[el.popup] || el.popup
          })
        )
      );
  }
  const pathTogglePopup = [
    "customAttributes",
    component.tag,
    "event",
    "togglePopup"
  ];
  const pathTogglePopupList = [
    "customAttributes",
    component.tag,
    "event",
    "togglePopupList"
  ];
  const valueTogglePopup = get__default["default"](component, pathTogglePopup);
  if (valueTogglePopup)
    set__default["default"](
      component,
      pathTogglePopup,
      mappingKeys[valueTogglePopup] || valueTogglePopup
    );
  const valueTogglePopupList = get__default["default"](component, pathTogglePopupList);
  if (valueTogglePopupList && Array.isArray(valueTogglePopupList))
    set__default["default"](
      component,
      pathTogglePopupList,
      valueTogglePopupList.map((el) => mappingKeys[el] || el)
    );
  return component;
};

exports.transformNestedContent = transformNestedContent;
