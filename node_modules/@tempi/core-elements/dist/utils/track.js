'use strict';

Object.defineProperty(exports, '__esModule', { value: true });

const getAttrTracking = (trackingAttr) => {
  if (!trackingAttr)
    return {};
  const { regionName, contentName, target, payload } = trackingAttr;
  return {
    "data-track-content": true,
    "data-content-region-name": regionName,
    "data-content-name": contentName,
    "data-content-target": target,
    "data-content-payload": payload
  };
};
const objectToAttrString = (obj) => {
  if (!obj || Object.keys(obj).length === 0)
    return "";
  const attributes = [];
  for (const [key, value] of Object.entries(obj)) {
    if (typeof value === "boolean") {
      attributes.push(`${key}=${value.toString().toLowerCase()}`);
    } else if (value !== void 0) {
      attributes.push(`${key}="${value}"`);
    }
  }
  return attributes.join(" ");
};

exports.getAttrTracking = getAttrTracking;
exports.objectToAttrString = objectToAttrString;
