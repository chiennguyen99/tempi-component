import { isUrlValid } from './isUrlValid.es.js';

const isGoogleSheetUrl = (url) => {
  if (!!url && isUrlValid(url)) {
    const matches = /\/d\/(.+)\/(.*?gid=(\d+))?/.exec(url);
    return !!matches;
  }
  return false;
};

export { isGoogleSheetUrl };
