'use strict';

Object.defineProperty(exports, '__esModule', { value: true });

var isUrlValid = require('./isUrlValid.js');

const isGoogleSheetUrl = (url) => {
  if (!!url && isUrlValid.isUrlValid(url)) {
    const matches = /\/d\/(.+)\/(.*?gid=(\d+))?/.exec(url);
    return !!matches;
  }
  return false;
};

exports.isGoogleSheetUrl = isGoogleSheetUrl;
