import { UnitEnum } from '@tempi/core-renderer';

const getSizeWithUnit = (value) => {
  if (typeof (value == null ? void 0 : value.size) === "number") {
    return `${value == null ? void 0 : value.size}${(value == null ? void 0 : value.unit) || UnitEnum.pixel}`;
  }
};

export { getSizeWithUnit };
