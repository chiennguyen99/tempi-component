'use strict';

var jsxRuntime = require('react/jsx-runtime');
var styled = require('@emotion/styled');
var coreRenderer = require('@tempi/core-renderer');
var React = require('react');

function _interopDefaultLegacy (e) { return e && typeof e === 'object' && 'default' in e ? e : { 'default': e }; }

var styled__default = /*#__PURE__*/_interopDefaultLegacy(styled);

function GradientBar({
  gradientColor,
  setSelectedGradientItem,
  onChange
}) {
  var _a, _b;
  const barRef = React.useRef(null);
  const handleDrag = (index, e) => {
    if (!(barRef == null ? void 0 : barRef.current))
      return;
    const barWidth = barRef.current.clientWidth;
    let startPos = e.clientX;
    barRef.current.onmousemove = (e2) => {
      const delta = Math.round((e2.clientX - startPos) * 100 / barWidth);
      const clone = [...gradientColor.colors];
      const endVal = clone[index].stop + delta;
      clone[index].stop += delta;
      if (Math.abs(delta) >= 1 && endVal >= 0 && endVal <= 100) {
        startPos = e2.clientX;
        clone[index].stop = endVal;
        onChange("colors", clone);
      }
    };
    barRef.current.onmouseup = () => {
      if (barRef.current.onmousemove)
        barRef.current.onmousemove = null;
    };
    barRef.current.onmouseleave = () => {
      if (barRef.current.onmousemove)
        barRef.current.onmousemove = null;
    };
  };
  return /* @__PURE__ */ jsxRuntime.jsx(TransparentBackground, {
    children: /* @__PURE__ */ jsxRuntime.jsx(GradientBarWrapper, {
      gradientColor,
      ref: barRef,
      children: (_b = (_a = gradientColor == null ? void 0 : gradientColor.colors) == null ? void 0 : _a.map) == null ? void 0 : _b.call(_a, (gColor, index) => /* @__PURE__ */ jsxRuntime.jsx(GradientItem, {
        gColor,
        onMouseDown: (e) => {
          setSelectedGradientItem(index);
          handleDrag(index, e);
        }
      }, index))
    })
  });
}
const TransparentBackground = styled__default["default"].div`
  border-radius: 8px;
  background-color: var(--background-color);
  background-image: url('data:image/svg+xml;charset=utf-8,<svg xmlns="http://www.w3.org/2000/svg" width="16" height="16" fill-opacity=".05"><path d="M8 0h8v8H8zM0 8h8v8H0z"/></svg>');
`;
const GradientBarWrapper = styled__default["default"].div(
  ({ gradientColor }) => `
      height: 12px;
      border-radius: 8px;
      margin-top: 12px;
      margin-bottom: 12px;
      position: relative;
      background: ${coreRenderer.stringifyColor({
    colorType: coreRenderer.ColorType.gradient,
    colorValue: gradientColor
  })};
      `
);
const GradientItem = styled__default["default"].div(
  ({ gColor: color }) => `
    height: 16px;
    width: 16px;
    padding-left: 2px;
    padding-right: 2px;
    position: absolute;
    z-index: 10000;
    background: ${color.color};
    left: min(max(${color.stop}%, 0%), 100%);
    top: 50%;
    transform: translate(-50%,-50%);
    border: 2px solid #fff;
    border-radius: 50%;
    box-shadow: 0 2px 4px rgb(0 0 0 / 20%);
    &:hover {
      cursor: pointer;
    }
  `
);

module.exports = GradientBar;
