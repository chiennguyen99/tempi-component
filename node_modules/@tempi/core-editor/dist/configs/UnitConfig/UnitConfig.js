'use strict';

Object.defineProperty(exports, '__esModule', { value: true });

var jsxRuntime = require('react/jsx-runtime');
var coreRenderer = require('@tempi/core-renderer');
var SelectConfig = require('../SelectConfig/SelectConfig.js');

const UnitConfig = ({
  propKey,
  options = [],
  defaultValue = coreRenderer.UnitEnum.pixel,
  onChange
}) => {
  return /* @__PURE__ */ jsxRuntime.jsx(SelectConfig.Select, {
    style: { width: 60 },
    propKey,
    defaultValue,
    options: options.map((unit) => ({
      value: unit,
      label: unit
    })),
    onChange
  });
};

exports.UnitConfig = UnitConfig;
