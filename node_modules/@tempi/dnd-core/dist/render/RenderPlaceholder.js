import { jsx as _jsx } from "react/jsx-runtime";
import { useEditor } from '../hooks';
export const RenderPlaceholder = ({ placeholder, suggestedStyles }) => {
    const { indicator } = useEditor((state) => ({
        indicator: state.options.indicator,
    }));
    return (_jsx("div", { style: Object.assign({ position: 'fixed', display: 'block', opacity: 1, borderColor: placeholder.error ? indicator.error : indicator.success, borderStyle: 'solid', borderWidth: '1px', zIndex: '99999' }, suggestedStyles) }));
};
