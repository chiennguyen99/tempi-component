import { jsx } from 'react/jsx-runtime';
import React from 'react';
import { DefaultRender } from './DefaultRender.es.js';
import { useInternalEditor } from '../editor/useInternalEditor.es.js';
import { useInternalNode } from '../nodes/useInternalNode.es.js';

const RenderNodeToElement = ({ render }) => {
  const { hidden } = useInternalNode((node) => ({
    hidden: node.data.hidden
  }));
  const { onRender } = useInternalEditor((state) => ({
    onRender: state.options.onRender
  }));
  if (hidden) {
    return null;
  }
  return React.createElement(onRender, { render: render || /* @__PURE__ */ jsx(DefaultRender, {}) });
};

export { RenderNodeToElement };
