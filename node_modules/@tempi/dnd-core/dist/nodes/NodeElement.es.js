import { jsx } from 'react/jsx-runtime';
import { NodeProvider } from './NodeContext.es.js';
import { RenderNodeToElement } from '../render/RenderNode.es.js';

const NodeElement = ({ id, render }) => {
  return /* @__PURE__ */ jsx(NodeProvider, {
    id,
    children: /* @__PURE__ */ jsx(RenderNodeToElement, {
      render
    })
  });
};

export { NodeElement };
