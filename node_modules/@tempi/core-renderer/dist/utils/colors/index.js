import generate from './generate';
const functionalColors = {
    primary: '#ee112e',
    secondary: '#0674e8',
    link: '#1990ff',
    pending: '#ffa940',
    success: '#43da71',
    error: '#da4343',
};
let presetPalettes = {};
Object.keys(functionalColors).forEach((key) => {
    presetPalettes = Object.assign(Object.assign({}, presetPalettes), generate(functionalColors[key], key));
});
export { generate };
export * from './util';
