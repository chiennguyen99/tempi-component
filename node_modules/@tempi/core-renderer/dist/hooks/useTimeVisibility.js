'use strict';

Object.defineProperty(exports, '__esModule', { value: true });

var React = require('react');
var dateTime = require('../utils/date-time.js');

const useTimeVisibility = (items, isEditingMode = false) => {
  const displayItems = React.useMemo(() => {
    return items == null ? void 0 : items.filter((item) => {
      return !isEditingMode ? dateTime.validateIsVisibleComponent((item == null ? void 0 : item.timeVisibility) || {}) : true;
    });
  }, [isEditingMode, items]);
  return {
    displayItems
  };
};

exports.useTimeVisibility = useTimeVisibility;
