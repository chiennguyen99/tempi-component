export type Env = 'local' | 'internal' | 'test-1' | 'dev' | 'stag' | 'production';
export type Device = 'mobile' | 'desktop';
export interface SSRInputs<T> {
    [key: string]: T[];
}
export interface SSROutputInterface<U = string, UU = {}> {
    slot?: U;
    serverData?: UU;
}
export interface SSRData<U = string, UU = {}> {
    [key: string]: SSROutputInterface<U, UU>[];
}
export type BuilderConfig = {
    env?: Env;
};
export type ComponentTagWithTeamPrefix<T, P extends string> = {
    [K in keyof T as K extends string ? `${P}_${K}` : never]: T[K];
};
//# sourceMappingURL=render.d.ts.map