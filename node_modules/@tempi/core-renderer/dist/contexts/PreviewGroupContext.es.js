import { jsx } from 'react/jsx-runtime';
import React, { useState, useContext } from 'react';
import { getImageUrlFromGoogleUserContent, preProcesssorGoogleImageParams } from '../utils/image.es.js';

var __defProp = Object.defineProperty;
var __defProps = Object.defineProperties;
var __getOwnPropDescs = Object.getOwnPropertyDescriptors;
var __getOwnPropSymbols = Object.getOwnPropertySymbols;
var __hasOwnProp = Object.prototype.hasOwnProperty;
var __propIsEnum = Object.prototype.propertyIsEnumerable;
var __defNormalProp = (obj, key, value) => key in obj ? __defProp(obj, key, { enumerable: true, configurable: true, writable: true, value }) : obj[key] = value;
var __spreadValues = (a, b) => {
  for (var prop in b || (b = {}))
    if (__hasOwnProp.call(b, prop))
      __defNormalProp(a, prop, b[prop]);
  if (__getOwnPropSymbols)
    for (var prop of __getOwnPropSymbols(b)) {
      if (__propIsEnum.call(b, prop))
        __defNormalProp(a, prop, b[prop]);
    }
  return a;
};
var __spreadProps = (a, b) => __defProps(a, __getOwnPropDescs(b));
const PreviewGroupContext = React.createContext({});
const PreviewGroupProvider = ({
  children,
  images
}) => {
  const [currentIndex, setCurrentIndex] = useState();
  const newSrcImages = images.map((image) => {
    const { src, googleImageParams } = image;
    const newSrc = getImageUrlFromGoogleUserContent(
      src,
      preProcesssorGoogleImageParams(googleImageParams, {
        format: "webp"
      })
    );
    const pngSrc = getImageUrlFromGoogleUserContent(
      src,
      preProcesssorGoogleImageParams(googleImageParams, { format: "png" })
    );
    return __spreadProps(__spreadValues({}, image), { src: newSrc || pngSrc || src });
  });
  return /* @__PURE__ */ jsx(PreviewGroupContext.Provider, {
    value: { images: newSrcImages, currentIndex, setCurrentIndex },
    children
  });
};
const usePreviewGroup = () => {
  const context = useContext(PreviewGroupContext);
  if (!context) {
    throw new Error(
      "usePreviewGroup must be inside a PreviewGroupContext with a state value"
    );
  }
  return context;
};

export { PreviewGroupProvider, usePreviewGroup };
